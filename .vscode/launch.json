{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--reload"],
      "python": "${workspaceFolder}/.venv/bin/python",
      "jinja": true,
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
